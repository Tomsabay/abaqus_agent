# Core dependencies (no Abaqus required for unit tests)
pyyaml>=6.0
jsonschema>=4.0

# Server (FastAPI for HTTP, MCP for Model Context Protocol)
fastapi>=0.100
uvicorn>=0.20
# Upper bound matches pyproject.toml's [project.optional-dependencies] mcp
# extra. `mcp_server.py` imports `mcp.server.fastmcp`, which moved in 2.0.0, so
# a clean install without this bound resolves to 2.x and the server fails on
# import. The two files are pinned together on purpose; changing one alone is
# how the bound went missing here in the first place.
mcp>=1.0,<2

# LLM backends (optional, install as needed)
# anthropic>=0.36
# openai>=1.0

# Testing
pytest>=7.0
pytest-cov>=4.0

# Code quality
ruff>=0.1
